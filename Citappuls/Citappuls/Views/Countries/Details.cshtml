@model Citappuls.Data.Entities.Country

@{
    ViewData["Title"] = "Details";
}
<div class="container mt-5">
    <h1>Detalles</h1>

    <partial name= "_CountryDetails" />
    <div>
        <a asp-action="Edit" asp-route-id="@Model?.Id" class="btn btn-outline-warning">Editar</a> 
        <a asp-action="Index"class="btn btn-outline-danger">Volver a la lista</a>
    </div>
</div>
